/**
 * Indicates the status of a ticket as open, work in progress (WiP) or closed.
 */

enum TicketStatus {
	open = 'OPENSTATUS',
	wip = 'WIPSTATUS',
	closed = 'CLOSEDSTATUS',
}

export default TicketStatus;
